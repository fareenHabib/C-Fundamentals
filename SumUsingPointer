#include <iostream>
using namespace std;
int main() {
    int a, b;
    cout << "Enter two numbers: ";
    cin >> a >> b;
    int *pa = &a, *pb = &b;
    int sum = *pa + *pb;
    cout << "Sum = " << sum << endl;
    return 0;
}
